<div class="centered column">
  <h1 class="ui header">Hi, {{user.displayName}}!</h1>
  <div class="ui segment">
    <p>ID: {{user.id}}</p>
    <p>Username: {{user.username}}</p>
    <p>Name:{{user.displayName}}</p>
  </div>
</div>